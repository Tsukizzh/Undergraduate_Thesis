# EZSpecificity 项目指令

本文件只放本项目的工作入口、证据规则和风险边界。长期项目背景已经归档到 `docs/PROJECT_CONTEXT_EZSpecificity.md`；需要研究历史时先读对应日志，不要把本文件当作最新进度记录。

## 一、事实来源优先级

回答项目事实时按下面顺序查证：

1. 用户在当前对话中的最新说明。
2. 实验目录里的脚本、配置、日志、结果文件和检查点。
3. `sessions/` 下对应会话日志。
4. `毕业设计/提取P450过程日志/项目进度日志.md`。
5. `docs/PROJECT_CONTEXT_EZSpecificity.md` 和旧研究手册。

如果这些来源冲突，以更接近原始产物、更接近当前日期的证据为准，并在回复中说明冲突。

## 二、关键入口

- 原始 EZSpecificity 代码：`src/`
- 原始模型与示例数据：`saved_model/`、`data/`
- P450 研究主目录：`毕业设计/P450_EZSpecificity_研究项目/`
- 长期项目背景归档：`docs/PROJECT_CONTEXT_EZSpecificity.md`
- 会话日志：`sessions/`
- 动态进度日志：`毕业设计/提取P450过程日志/项目进度日志.md`

根据已归档背景，P450 专项研究曾进入 PathC/PathD 后续实验阶段。用户已说明 EXP006 是“按照序列相似度划分”的实验，若继续处理 EXP006，必须先查对应实验目录和会话日志确认最新状态。

## 三、必须查证的任务

以下任务必须先查原始证据，不凭记忆回答：

1. ESM、GROVER、LMDB、缓存、索引、样本划分、负样本、数据泄漏、样本污染、特征错配。
2. AUC、AUPR、验证集/测试集结论、历史实验是否作废、结果能否写入论文。
3. Fe、HEM、P450、底物分类、对接结构、口袋原子、催化距离筛选。
4. 是否重跑训练、是否中断服务器任务、是否保留或删除检查点。
5. 修改数据处理脚本、训练脚本、评估脚本或实验配置。
6. ColabFold / AlphaFold 结构预测、AlphaFill 补辅因子、Uni-Dock / Vina 对接、MMseqs2 聚类、GPU 加速、批处理和任何高成本服务器流程。

涉及上述内容时，最终回复要区分：

- 已由文件、日志或命令验证的事实。
- 由代码或实验设计推断出的判断。
- 仍未确认的不确定性。

涉及外部工具和高成本计算时，还必须先查官方文档、本机 `--help`、已有最佳实践和当前日志，确认最高效且适合本任务的入口，再启动正式全量任务。严禁闭门造车、只按经验写脚本或在没有小批性能基准的情况下长时间占用昂贵服务器资源。

P450 结构和对接任务的硬规则：

1. ColabFold / AlphaFold 预测结构默认没有 HEM/Fe，任何用于 P450 对接、Fe-催化原子距离、口袋定位或湿实验打分的流程，都必须在设计阶段写明 HEM/Fe 来源、补充方法、失败处理和质量标签。
2. AlphaFill 不能只用少数模板粗略覆盖所有候选。必须优先查 AlphaFill 数据库、API、下载数据或完整候选模板库，并记录每条候选的模板来源、identity、local RMSD、clash、Fe-Cys 距离和置信度。少数模板 overlay 只能作为临时应急结果，不能包装成正式结论。
3. Uni-Dock 正式运行前必须查官方 README、服务器 `unidock --help` / `unidocktools unidock_pipeline --help` 和当前任务日志，先按任务形态选择官方入口，不能只盯一个参数。单 receptor / 单 pocket 对大量 ligand 时，优先评估 `unidock --receptor ... --gpu_batch ...`；ligand 很多或命令行过长时，用路径列表形式的常规 `--ligand_index` 替代 `--gpu_batch`。多 receptor / 多 pocket 对单 ligand 或少量 ligand，且每条配对有独立 box 时，再评估 `--paired_batch_size` 和 JSON `--ligand_index`，并确认本机版本、Vina scoring 限制和 JSON schema。从 PDB/SDF 等常见格式出发、需要蛋白/配体准备、补氢或统一工作目录时，评估 `unidocktools proteinprep`、`ligandprep`、`unidock_pipeline`。只打分或局部优化时评估 `--score_only` / `--local_only`。每次全量前同步核对 `--search_mode` 或显式 `--exhaustiveness/max_step`、`--num_modes`、`--refine_step`、`--max_gpu_memory`、`--scoring`、box 参数、输入格式、输出目录、GPU 利用率和每样本耗时。不能默认每个 receptor 单独启动一个低吞吐命令。
4. ColabFold 大批量预测要先判断是否依赖公共 MSA server、是否会限流、是否需要本地 `colabfold_search` / MMseqs2 数据库、序列去重、分片并行或复用已有 AlphaFold / AlphaFill 结构。
5. 正式全量前必须做小批真实性能基准，报告 GPU 利用率、显存、每样本耗时、主要瓶颈和预计总耗时；出现限流、低利用率、残留孤儿进程、异常慢日志或异常分数时，先暂停排查。

## 四、实验目录边界

1. 默认只改用户指定的实验目录或脚本，不跨实验目录同步改动。
2. PathC 的各 EXP 目录通常用于架构或训练隔离，改动前先确认当前实验是否应独立维护。
3. 共享缓存、共享特征、全局脚本和原始数据属于高风险资产，修改前必须先说明影响范围。
4. 不删除、不覆盖、不重建大规模数据或远程产物，除非用户明确授权。

## 五、代码修改边界

1. 修改脚本、配置、模型或数据处理流程前，先读相关代码、配置、日志和调用链；证据不足时再扩大范围。
2. 只改用户目标需要的范围，不顺手重构无关代码，不还原用户已有改动。
3. 手工编辑优先使用补丁工具；不要用脚本随意重写人工内容。
4. 工具失败后先判断是否是权限、网络、路径或环境问题，不要把工具失败解释成实验事实。
5. 结构性删改、目录整理、批量删除、跨目录迁移或大范围重写前，先检查 Git 状态，必要时提交或推送安全锚点，并说明可回退位置。

## 六、验证要求

代码、脚本、实验或论文结论改动后，按任务影响选择最小必要验证：

1. 文档或目录整理：检查路径存在、链接和文件名正确。
2. 数据处理脚本：抽样核对输入输出行号、键、样本数和关键字段。
3. 训练或评估脚本：优先跑轻量级冒烟测试，再决定是否上服务器。
4. 研究结论和每次实验相关任务：必须给出证据路径，并使用子智能体协作复核；复核结果要和原始证据一起汇报。

不能运行验证时，要说明原因和剩余风险。

## 七、研究审计与实验诊断

1. 遇到研究审计、错误诊断或实验决策时，先界定问题、影响范围和初步假设。
2. 收集实验目录、脚本、配置、日志、结果文件、检查点和服务器当前状态等原始证据，再使用子智能体协作复核或讨论。
3. 子智能体结论不能替代主助手核对；关键判断必须回到原始证据。
4. 最终答复说明复核是否参与、结论是否一致、关键证据和剩余不确定性。
5. 重要实验、目录调整、关键结论、错误发现、决策依据和下一步安排，应写入对应 `session_log` 或项目进度日志。

## 八、子智能体协作

1. 这里的子智能体特指当前 Codex 环境中、用户界面右上角可见的自有 subagent（例如 Socrates/Darwin 对应的 explorer、worker 等）；不得用 Codex MCP、`collaborating-with-codex` 或其他外部 Codex 会话替代，除非用户明确要求使用这些工具。
2. 调用子智能体前，先判断是否已有相关子智能体在同一问题上积累上下文；如果问题延续、需要追问、需要复用前次分析或需要保持多轮讨论连贯，优先继续向原来的子智能体发送输入；如果任务独立、上下文过期、写入范围不同或复用会造成混淆，再新建子智能体，并说明原因。
3. 凡是涉及实验、数据、代码改动、论文结论、容易出错的路径/命令/口径判断，或需要讨论方案的任务，默认使用可用子智能体参与协作；子智能体不是权威结论，主助手必须回到原始证据核对后再判断。
4. 每次实验相关任务都要安排子智能体复核，覆盖实验设计、数据口径、配置、结果解释、是否能写入论文和需求完成度。
5. 子智能体合作不只用于代码审查，也用于多轮头脑风暴、方案比较、反证搜索、论文表述取舍、实验设计讨论和需求完成度检查。
6. 给子智能体的任务要明确边界、证据要求和是否允许修改文件；默认审查、讨论和头脑风暴任务只读，不改文件。
7. 多个子智能体结论冲突时，先核对原始文件、日志、配置、结果表或服务器当前状态，再汇报一致点、冲突点和主助手判断。
8. 汇报时说明哪些结论来自子智能体讨论，哪些已经由主助手复核为事实；不要把子智能体意见包装成已验证事实。

## 九、远程服务器

涉及北京服务器、远程训练、上传下载、部署或查看远程日志时，使用已配置的 SSH 技能或对应脚本，不直接手写裸 SSH/SCP 命令。远程删除、覆盖、重跑训练、中断进程、关机前必须先向用户说明操作对象和风险。

服务器上的原有文件默认按只读资产处理：未经用户明确确认，不修改、不覆盖、不删除原文件内容；需要产出新结果时，优先新建带时间戳、版本号或说明性后缀的文件、目录、日志或备份副本，在原有基础上增量追加。确需改动原文件时，先说明文件路径、改动范围、回滚方案和不可逆风险，并得到用户确认。

## 十、常用项目判断

1. 不把旧实验日志中的“当前阶段”直接当作今天的当前状态。
2. 不把历史 AUC/AUPR 结论移植到新数据划分、新缓存或新脚本上。
3. 看到异常提升、异常下降或“结果反转”时，优先怀疑数据对齐、样本划分、缓存键和评估口径。
4. 涉及论文表述时，宁可写保守结论，也不要把未验证推断包装成确定事实。

## 十一、表达偏好

1. 避免机械套用被明显识别为 AI 腔的句式，尤其是“不是……而是……”“不只是……更是/还要……”这类对照否定句；确需表达转折时，直接说清事实、原因和结论。
2. 不要高频使用长破折号 `—`、双连字符 `--` 或类似符号制造转折、递进和戏剧感；正常标点够用时优先用逗号、句号或分号。
3. 少用空泛套话和填充句，例如“值得注意的是”“关键在于”“在当今快节奏/不断变化的……中”“这不仅关乎……”，除非它们确实承载具体信息。
