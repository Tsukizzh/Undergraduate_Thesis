# Q2 独立 Codex 复核记录

日期：2026-05-21

复核方式：独立 Codex 只读复核

SESSION_ID：`019e4a27-dc49-7e41-9524-0b16b20c67e1`

## 复核结论

未发现当前 Q2 已完成步骤的实质错误。现有证据支持：

- FASTA 导出、MMseqs2 聚类、cluster 解析、样本回填这条链路表面闭合。
- 1622 个酶都进入且只进入一个 cluster。
- `data.csv` 总样本数、正负样本数、按 cluster 回填后的总数一致。
- `enzyme_index` 没有缺失或越界。
- 硬校验脚本结果为 0 errors、0 warnings。

## 需要补做的硬审计

正式生成 train/val/test split 前，必须补做：

1. 追溯 `data.csv` 与 `Enzymes.csv` 的生成来源，证明 `enzyme` 整数索引在语义上等于当前 `Enzymes.csv` 行号。
2. 对 11 组精确重复序列加硬校验，确认所有候选阈值下都没有被拆到不同 cluster。
3. 输出长度异常序列清单，例如 <300 aa、>800 aa，判断是否为片段、融合蛋白或注释异常。
4. 保存 MMseqs2 版本、完整命令和原始 TSV，保证后续可复现。
5. 正式 split 前做多随机种子可行性模拟，审计样本数、正负样本、酶数、cluster 数、最大 cluster 占比和阳性率偏差。
6. 正式 split 后审计同一 enzyme、同一精确序列、同一 MMseqs2 cluster 不跨 train/val/test，并量化跨划分最近邻序列相似度。
7. 记录底物侧重叠，因为酶序列聚类只能降低酶相似性泄漏，不能解决同一底物或相似底物跨划分带来的泄漏。

## 阈值判断

- id30 过粗的证据较强：最大簇 481 个酶、12990 个样本、982 个正样本。
- id80 不应简单等同于随机划分，它仍能挡住高同源和精确重复泄漏，但泛化压力较弱。
- id60 更适合作为主方案起点。
- id40 更严格，适合作为压力更大的并行方案。
- 最终阈值应由 split 可行性模拟和跨划分最近邻相似度审计决定，而不是只看 cluster 数量。
