# P450研究项目组会汇报材料 v2

---

## 目录

0. [背景回顾：四路径规划与配体处理方案](#零背景回顾四路径规划与配体处理方案)
1. [实验二v2：反馈与数据重构](#一实验二v2反馈与数据重构)
2. [Path A Step 1-3：数据准备阶段](#二path-a-step-1-3数据准备阶段)
3. [Path A Step 4-8：特征生成阶段](#三path-a-step-4-8特征生成阶段)
4. [Path A Step 9-10：模型推理与结果分析](#四path-a-step-9-10模型推理与结果分析)
5. [改进方向](#五改进方向)

---

## 零、背景回顾：四路径规划与配体处理方案

### 0.1 P450研究四路径规划

| 路径 | 目标 | 前置条件 | 状态 |
|------|------|----------|:----:|
| **A** | 用PDB实验结构评估EZSpecificity模型（初步尝试） | 无 | ✅ 已完成 |
| B | 构建P450专属训练数据集（以路径A为基础扩大样本量） | 路径A完成 | ⏳ 待定 |
| C | 训练或微调P450专属模型 | 路径B完成 | ⏳ 待定 |
| D | 区域选择性预测 | 路径A效果好 | ⏳ 待定 |


---

**路径A执行步骤**：

| 阶段 | 步骤 | 任务 | 描述 | 产出 |
|------|------|------|------|------|
| **数据准备** | Step 1 | 下载PDB文件 | 从RCSB下载627个P450晶体结构 | pdb_files/ |
| | Step 2 | 提取酶序列 | 从PDB提取292条唯一酶序列 | Enzymes.csv |
| | Step 3 | 配体处理与数据集构建 | 三方深度验证（Claude+Codex+Gemini）配体分类，生成6套方案 | Substrates.csv + data.csv |
| | Step 4 | CSV格式修正 | 统一列名、列顺序，确保与模型输入要求一致 | 修正后的CSV文件 |
| **特征生成** | Step 5 | ESM酶嵌入生成 | ESM-2模型提取1280维蛋白序列特征 | enzyme_features.lmdb |
| | Step 6 | 反应图特征生成 | 从SMILES构建分子图，提取拓扑特征 | reaction_features.lmdb |
| | Step 7 | 分子指纹生成 | Morgan指纹（1024-bit）+ GROVER嵌入（2400+4885维） | 多个特征文件 |
| | Step 8 | 结构特征生成 | 提取10Å口袋，配体对齐，生成结构特征 | structure_features.lmdb |
| **预测分析** | Step 9 | 模型推理 | 使用best-checkpoint对517条数据预测特异性 | predictions.csv |
| | Step 10 | 结果分析 | 计算AUC-ROC/AUC-PR，三方协作根因分析 | 评估报告 |

### 0.2 路径A Step 3 的关键问题：多配体处理

**问题发现**：一个PDB结构中可能包含多个配体（底物、产物、抑制剂混合）

例如：某个PDB结构中同时包含底物和产物，或者同时包含底物和抑制剂，这些都会被提取为独立的酶-配体对。

**问题根源：ESIBank vs 我们的流程差异**

| 方面 | ESIBank | 我们 |
|------|---------|------|
| **数据流** | 关系优先→结构其次 | 结构优先→关系推断 |
| **起点** | BRENDA酶-底物关系（文献验证） | PDB实验结构 |
| **对接** | AutoDock一次一个底物 | 直接使用PDB配体 |
| **配体来源** | 已知是底物 | 可能是底物/产物/抑制剂 |

- **ESIBank从BRENDA获取"酶A催化底物X"的关系，然后为每个关系单独做AutoDock对接，每个复合物天然只有一个目标配体**
- 我们直接从PDB拿实验结构，PDB是晶体学实验的记录，配体是实验条件的产物，可能同时有底物+产物+抑制剂
  - 例如：P450cam（CYP101A1）的晶体结构中可能同时包含樟脑（底物）和5-羧基樟脑（产物），这两个都会被提取为独立的酶-配体对

**三种解决方案**

**方案A：严格筛选（只用共晶底物）**
- **做法**：查阅文献，逐一确认每个PDB配体是否为该酶的真正底物，只保留确认是底物的酶-配体对
- **优点**：数据质量最高，标签可靠，预测结果可信度高
- **缺点**：需要大量人工查阅文献，耗时较长，数据量会减少约40%


**方案B：多配体展开（直接使用）**
- **做法**：不区分底物/产物/抑制剂，把所有提取到的每个配体都当作单独的"酶-配体对"输入模型
- **优点**：最快速，不需要额外工作，可以立即开始后续流程
- **缺点**：标签可能不准确（产物/抑制剂被当作底物），但可以观察模型对不同类型配体的预测差异


**方案C：虚拟对接（ESIBank风格）**
- **做法**：去掉PDB中的配体，查阅文献或其他数据库找到该酶的真正底物，然后用AutoDock将真正底物对接到我们的PDB实验结构（酶）上
- **优点**：酶结构是实验解析的高质量结构，底物标签可靠（文献验证），与ESIBank训练数据生成方式一致
- **缺点**：需要查阅文献确认底物、额外的对接工作，计算耗时最长


**执行计划**：B → A → C（先快速验证，再按需精细化）

### 0.3 开会后反馈：方案B需要细化

> "方案B按配体类型拆分成多个子方案"

因此我们将方案B拆分为：

| 子方案 | 包含的配体类型 | 说明 |
|:------:|---------------|------|
| **B1** | 仅 SUBSTRATE（底物） | 最严格，符合EZSpecificity原始定义 |
| **B2** | SUBSTRATE + PRODUCT | 包含代谢产物 |
| **B3** | SUBSTRATE + PRODUCT + INHIBITOR | 最宽松，测试模型边界 |

---

## 一、实验二v2：数据重构

### 1.1 上次开会时的两条修改意见

| 编号 | 修改内容 | 原方案 (v1) | 新方案 (v2) |
|:----:|----------|-------------|-------------|
| 1 | B方案拆分 | 不区分配体类型（方案B） | 拆成B1/B2/B3三种子方案 |
| **2** | **去重规则** | **按 UniProt ID 分组** | 按 **(UniProt + 配体)** 分组 |


```
                               上一个版本

输入数据                    ┌─────────────────┐
(RCSB搜索结果)              │  1,591个 实体   │
                           └────────┬────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ Step 1: 质量筛选                                                     │
│         ├── 分辨率 ≤ 3.0 Å（排除模糊结构）                            │
│         └── 序列长度 300-1200 aa（排除片段/融合蛋白）                 │
├─────────────────────────────────────────────────────────────────────┤
│         排除：7个无分辨率 + 99个低分辨率 + 59个短序列                  │
│         结果：1,426个                                                │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ Step 2: 配体筛选                                                     │
│         ├── 必须有非辅因子配体（真正的底物/抑制剂/药物）                │
│         └── 排除仅有HEM/水/缓冲盐的结构                               │
├─────────────────────────────────────────────────────────────────────┤
│         排除列表（50+化合物）：                                       │
│         ├── 血红素：HEM, HEC, HEA, HEB, HAS                          │
│         ├── 辅酶：FAD, FMN, NAD, NAP, NDP                            │
│         ├── 水/离子：HOH, H2O, NA, CL, K, MG, CA, ZN, FE             │
│         ├── 缓冲盐：SO4, PO4, ACT, EDO, TRS                          │
│         ├── 实验添加剂：GOL, PEG, PGE, MPD, DMS, 1PE, P6G            │
│         └── 醇类溶剂：IPA, MOH, EOH, IPH                             │
├─────────────────────────────────────────────────────────────────────┤
│         排除：280个仅有辅因子的结构                                   │
│         结果：1,146个                                                │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│ Step 3: 去冗余                                                       │
│         ├── 按UniProt ID分组                                         │
│         └── 每组只保留分辨率最好的那个结构                             │
├─────────────────────────────────────────────────────────────────────┤
│         去除：960个冗余结构                                          │
│         结果：186个独特的P450                                        │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
                           ┌─────────────────┐
去冗余后                    │   186个 P450    │
                           └────────┬────────┘
                                    │
                    （后续：数据泄露排除 → 158个）
```

**修改原因**：
- **修改1**：不同类型的配体（底物/产物/抑制剂）具有不同的研究价值，需要分开评估
- **修改2**：同一个酶可以与多个不同配体结合，原方案会丢失大量有价值的酶-配体对数据

### 1.2 数据量变化

这是数据管道的**上游改动**，后续所有步骤都需重做：

| 指标 | v1 版本 | v2 版本 | 说明 |
|------|---------|---------|------|
| 总记录数 | 158条 | **682条** | v2一个UniProt可对应多条记录 |
| 唯一 PDB 数 | 158个 | **626个** | v2一个UniProt可有多个PDB |
| 唯一 UniProt 数 | ~158个* | **153个** | v2排除了Photosystem I污染（58条记录） |
| 唯一酶序列数 | 158个 | **292个** | PDB序列≠UniProt规范序列（见下方说明） |
| Step 2后唯一UniProt | - | **148个** | 153→148：5对UniProt共享相同序列，去重后合并 |



> **为什么153个UniProt能产生292个唯一序列？**
>
> 这是**正常且合理的**，因为：
> - **PDB中的序列**是实验室表达构建物的序列，不是UniProt数据库中的规范（canonical）序列
> - **序列差异来源**：His-tag纯化标签（28.7%的序列含有`HHHHHH`）、N端截断、突变体、不同表达载体
> - **具体分布**：105个UniProt（69%）只有1个序列；48个UniProt（31%）有2-36个不同序列
> - **典型案例**：P14779（P450 BM3）有78条记录、69个PDB、36个唯一序列（长度455-494 aa）

---

## 二、Path A Step 1-3：数据准备阶段

### 2.1 Step 1：下载 PDB 文件

从 RCSB PDB 数据库下载 627 个结构文件：

| 指标 | 数值 |
|------|------|
| 目标 PDB 数 | 627 |
| .pdb 格式 | 619 |
| .cif 格式 | 8 |
| 从 v1 复用 | 145 |
| 新下载 | 482 |

### 2.2 Step 2：提取酶序列

从 682 条记录中提取唯一酶序列：

| 指标 | 数值 |
|------|------|
| 总记录数 | 682 |
| 唯一酶序列数 | 292 |
| 唯一 UniProt 数 | 148（源数据153个，5对共享相同序列被合并） |
| 平均序列长度 | ~450 aa |

**产出文件**：
- `Enzymes.csv`：292 条唯一酶序列（对应 148 个 UniProt）
- `record_enzyme_mapping.csv`：记录与酶的映射关系

### 2.3 Step 3：配体处理与数据集构建（重点）

Step 3 是整个流程中**工作量最大**的步骤，包含 5 个子任务。其中**子任务 03 三方深度验证**是核心。

#### 概览

| 子任务 | 名称 | 状态 | 说明 |
|:------:|------|:----:|------|
| 01 | 初步分类 | ✅ | 基于规则的初步分类 |
| 02 | 并行预审 | ✅ | 快速预审，数据不够准确 |
| **03** | **三方深度验证** | ✅ | **核心任务，见下方详解** |
| 04 | 提取 SMILES | ✅ | 从 CCD 提取配体结构 |
| 05 | 数据集切分 | ✅ | 生成 B1-B6 六套方案 |

---

### 子任务 03：三方深度验证

这是整个项目中**投入最大**的任务，我们使用 12 个并行窗口，每个窗口调用 3 个 AI API 对每条记录进行深度验证。

```
┌─────────────────────────────────────────────────────────────┐
│                    三方深度验证架构                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   总记录数：682 条                                           │
│   并行窗口：12 个（每个处理约 57 条）                         │
│   每条记录调用：Claude API、Codex API、Gemini API            │
│   每个窗口执行时间：约 26 小时（并行）                           │
│                                                             │
│   ┌─────────┐    ┌─────────┐    ┌─────────┐                │
│   │ Window 1│    │ Window 2│    │   ...   │    ...共12个    │
│   │ (~57条) │    │ (~57条) │    │ (~57条) │                │
│   └────┬────┘    └────┬────┘    └────┬────┘                │
│        │              │              │                      │
│        ▼              ▼              ▼                      │
│   ┌─────────────────────────────────────────────┐          │
│   │           每条记录的验证流程                  │          │
│   │                                             │          │
│   │  ┌──────────────┐                           │          │
│   │  │ Claude Code  │ ← 最终决策、协调三方        │          │
│   │  └──────┬───────┘                           │          │
│   │         │  分发任务 + 综合结果                │          │
│   │  ┌──────┴───────┐                           │          │
│   │  ▼              ▼                           │          │
│   │  ┌────────┐  ┌────────┐                     │          │
│   │  │ Codex  │  │ Gemini │                     │          │
│   │  │结构分析│  │文献搜索│                      │          │
│   │  └────────┘  └────────┘                     │          │
│   │                                             │          │
│   │  三方分工：                                  │          │
│   │  ├─ Claude Code（决策者）：                   │          │
│   │  │  综合三方意见，做出最终分类决定             │          │
│   │  ├─ Codex（结构分析师）：                     │          │
│   │  │  分析 Fe-配体距离、结合模式、共价键判断等    │          │
│   │  └─ Gemini（文献检索员）：                    │          │
│   │     搜索 PMID、Ki/IC50 数据、催化证据等        │          │
│   │                                             │          │
│   │  每条记录验证内容：                           │          │
│   │  ├─ 配体角色：SUBSTRATE / PRODUCT /          │          │
│   │  │            INHIBITOR / EXCLUDE            │          │
│   │  ├─ 文献证据：PMID、Ki/IC50等                 │          │
│   │  ├─ 结构证据：Fe-配体距离、共价键等            │          │
│   │  └─ 置信度：HIGH / MEDIUM / LOW              │          │
│   └─────────────────────────────────────────────┘          │
│                                                             │
│   验证成果：                                                │
│   ├── 发现标注错误：若干                                     │
│   │   ├── 分类错误（SUBSTRATE ↔ INHIBITOR 误判）             │
│   │   └── 排除遗漏（应排除的记录未被标记）                    │
│   ├── 三方一致率：87.8%（599/682）                           │
│   └── 证据等级 A+B：91.2%（622 条有文献支持）                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

### 子任务 04：提取 SMILES

从 CCD (Chemical Component Dictionary) 提取配体的 SMILES 结构：

| 指标 | 数值 |
|------|------|
| 唯一 CCD 代码数 | 543 |
| 成功提取 | 543 (100%) |
| 最终唯一底物 SMILES | 436 |

### 子任务 05：数据集切分

根据三方深度验证的配体分类结果，生成 6 套评估方案用于不同的研究目的：

**基础数据（来自子任务03三方深度验证）**：
- SUBSTRATE（底物）：271条
- PRODUCT（产物）：23条
- INHIBITOR（抑制剂）：245条
- EXCLUDE（排除）：143条
- 总计：682条

**6套评估方案详解**：

| 方案 | 正样本构成 | 负样本构成 | 样本数 | 正负比 | 评估目的 |
|:----:|-----------|-----------|:------:|:------:|---------|
| **B1** | SUBSTRATE (271) | — | 271 | — | 仅底物，用于正样本基线 |
| **B2** | SUBSTRATE (271) | PRODUCT (23) | 294 | 11.8:1 | 底物正、产物负，测试底物-产物区分能力 |
| **B3** | SUBSTRATE (271) | PRODUCT + INHIBITOR (268) | 539 | 1.01:1 | **实际使用**：完整数据集，正负均衡 |
| **B4** | — | PRODUCT (23) | 23 | — | 仅产物，用于负样本分析 |
| **B5** | — | INHIBITOR (245) | 245 | — | 仅抑制剂，用于负样本分析 |
| **B6** | SUBSTRATE (271) | INHIBITOR (245) | 516 | 1.1:1 | 底物正、抑制剂负，排除产物 |

**方案B3的详细构成**（实际用于Step 9推理）：
```
总样本：539条 → 结构特征提取成功：517条 (95.9%)
├── 正样本：261条
│   └── SUBSTRATE（底物）：261条
└── 负样本：256条
    ├── PRODUCT（产物）：~22条
    └── INHIBITOR（抑制剂）：~234条
```

**后续使用 B3 方案**进行模型评估，原因：
1. **正负样本均衡**：正负比1.01:1，避免类别不平衡问题
2. **负样本多样性**：包含产物（化学结构相似）和抑制剂（能结合但不催化），比ESIBank的随机配对更具挑战性
3. **数据完整性**：95.9%的记录有完整结构特征，样本损失率低

---

## 三、Path A Step 4-8：特征生成阶段

### 3.1 Step 4：CSV 格式修正

确保数据文件符合模型输入要求：

| 文件 | 修正内容 |
|------|----------|
| data.csv | 列顺序调整为：Dock Index, Enzyme Index, Substrate Index, Label |
| Enzymes.csv | 确保列名为 "Protein sequence" |
| Substrates.csv | 确保列名为 "Substrate_SMILES" |

### 3.2 Step 5：ESM 酶嵌入生成

使用 ESM-2 模型生成蛋白质序列嵌入：

| 指标 | 数值 |
|------|------|
| 模型 | esm2_t33_650M_UR50D (650M 参数) |
| 输出维度 | 1280 维 per residue |
| 输入序列数 | 292 条 |
| 成功数 | 292 条 (100%) |
| 输出大小 | ~700 MB (enzyme_features.lmdb) |
| 硬件 | NVIDIA 4070 Super GPU |

### 3.3 Step 6：反应图特征生成

| 指标 | 数值 |
|------|------|
| 输入底物数 | 436 |
| 成功数 | 436 (100%) |
| 输出 | reaction_features.lmdb |

### 3.4 Step 7：分子指纹生成

**7.1 Morgan 指纹**

| 指标 | 数值 |
|------|------|
| 维度 | 1024-bit |
| 输出形状 | (436, 1024) |
| 数据类型 | int8 |
| 文件大小 | 436.1 KB |

**7.2 GROVER 指纹**

| 指标 | 数值 |
|------|------|
| 处理底物数 | 436 |
| embedding 维度 | 2400 |
| total 维度 | 4885 |
| 输出大小 | ~10 GB (LMDB) |

### 3.5 Step 8：结构特征生成

**8.1 口袋/配体提取**

| 指标 | 数值 |
|------|------|
| 输入 PDB 数 | 539 |
| 口袋提取成功 | 526 (97.6%) |
| 配体对齐成功 | 517 (95.9%) |
| 口袋半径 | 10 Å |

**8.2 LMDB 生成**

| 指标 | 数值 |
|------|------|
| 输入 | 517 条 |
| 成功 | 517 条 (100%) |
| 输出 | structure_features.lmdb |
| high_quality_id.txt | 517 个 ID |

---

## 四、Path A Step 9-10：模型推理与结果分析

### 4.1 使用的模型检查点（重要）

我们使用的是 EZSpecificity 论文发布的预训练模型：

```
┌─────────────────────────────────────────────────────────────┐
│                    模型检查点信息                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   文件名：best-checkpoint.ckpt                               │
│   文件大小：22 MB                                            │
│   训练时间：2023-12-30 ~ 2023-12-31                          │
│                                                             │
│   训练数据：                                                 │
│   ├── BRENDA 数据库（主要来源）                              │
│   ├── 包含 389 个 P450 酶（来自 BRENDA）                     │
│   └── 7 个小家族酶                                          │
│       ├── Duf                                               │
│       ├── Esterase                                          │
│       ├── Gt_acceptor                                       │
│       ├── Nitrilase                                         │
│       ├── Phosphatase                                       │
│       ├── Thiolase                                          │
│       └── halogenase                                        │
│                                                             │
│   验证集性能：                                               │
│   ├── AUC-ROC：0.8878                                       │
│   └── AUC-PR：0.6141                                        │
│                                                             │
│   关键特点：                                                 │
│   ├── 训练集含 389 个 P450（BRENDA 来源）                     │
│   └── ⚠️ 与我们测试集的 148 个 P450 完全不重叠（0%）          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**模型架构**：
- SE(3)-等变图神经网络 (EGNN)
- 交叉注意力机制
- 输入特征：ESM 嵌入 + Morgan 指纹 + GROVER 指纹 + 结构特征

### 4.2 推理执行

| 指标 | 数值 |
|------|------|
| 使用数据集 | B3 方案 |
| 总样本数 | 539 条 |
| 高质量样本（有结构特征） | **517 条** |
| 正样本 | 261 条 |
| 负样本 | 256 条 |

### 4.3 评估结果

```
┌─────────────────────────────────────────────────────────────┐
│                    P450 测试集评估结果                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   核心指标：                                                 │
│   ┌────────────────────────────────────────────┐            │
│   │  AUC-ROC：0.6636                           │            │
│   │  AUC-PR：0.6360                            │            │
│   └────────────────────────────────────────────┘            │
│                                                             │
│   其他指标（默认阈值 0.5）：                                  │
│   ├── Accuracy：0.5164                                      │
│   ├── Recall：0.0996（极低）                                 │
│   └── 最优阈值（Youden's J）：0.0379                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 4.4 与论文基准对比

| 场景 | AUC-ROC | 说明 |
|------|---------|------|
| 论文 Random Split | 0.8878 | 酶和底物都见过 |
| 论文 Unknown enzyme | 0.7741 | 酶未见过 |
| 论文 Unknown substrate | 0.8003 | 底物未见过 |
| 论文 Unknown enzyme+substrate | **0.7198** | 酶和底物都未见过 |
| **我们的 P450 测试** | **0.6636** | 低 5.6% |

### 4.5 根因分析（三方协作）

我们组织 Claude Code、Codex、Gemini 三方协作，深入分析 AUC-ROC 低于预期的原因：

#### 主因：任务不匹配

```
ESIBank 训练集的负样本生成方式：
├── 正样本：酶 A 催化底物 X（文献证实）
└── 负样本：随机选择酶 B 配对底物 X（假设不催化）

我们测试集的负样本来源：
├── 正样本：酶 A 催化/结合配体 X（PDB 实验结构）
└── 负样本：酶 A 结合抑制剂/产物 Y（也是 PDB 实验结构！）

关键区别：
├── ESIBank 负样本：酶和底物"完全无关"（容易区分）
└── 我们的负样本：酶和抑制剂/产物"高度相关"（难以区分）

结论：抑制剂/产物与底物可能化学结构相似，模型难以区分
```

#### 次因：测试集 P450 未被训练集覆盖

| 指标 | 数值 |
|------|------|
| 测试集 P450 UniProt ID | 148 个 |
| 训练集 P450 UniProt ID | 389 个（BRENDA 来源） |
| 两者重叠 | **0 个 (0%)** |
| 结论 | 测试集的 148 个 P450 是训练集未见的新酶 |

#### 次因：域偏移

| 方面 | ESIBank | 我们 |
|------|---------|------|
| 配体来源 | BRENDA 底物（文献记录的催化底物） | PDB 配体（实验结构中的任何结合分子） |
| 配体类型 | 几乎全是底物 | 底物、产物、抑制剂混合 |
| 化学空间 | 偏向可催化的小分子 | 偏向可结晶的分子（通常更稳定） |

### 4.6 结论

```
┌─────────────────────────────────────────────────────────────┐
│                         核心结论                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   1. AUC-ROC 0.6636 在预期范围内                             │
│      └── 比论文最难场景 (0.7198) 低 5.6%                     │
│                                                             │
│   2. 差距主要来自任务定义不同                                 │
│      └── 我们用抑制剂+产物做负样本，比随机配对更难区分          │
│                                                             │
│   3. 这不代表模型"失败"                                      │
│      └── 而是评估任务本身与训练任务不同                       │
│                                                             │
│   4. 模型在这批未见过的 P450 酶上仍有一定泛化能力             │
│      └── AUC-ROC > 0.5 表明预测优于随机                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 五、改进方向

基于以上分析，我们提出三条改进路径：

### 5.1 路径 1：随机配对负样本测试（验证假说）

**目标**：验证"任务不匹配"是否为主因

**方法**：
1. 保持正样本不变（271 条真实酶-底物对）
2. 生成新负样本：随机配对 P450 酶和非催化底物
3. 重新评估 AUC-ROC

**预期**：
- 如果假说正确，AUC-ROC 应提升至 0.72+
- 证明模型本身没有问题，是评估任务设计不当

**工作量**：1-2 天

### 5.2 路径 2：P450 专属模型微调

**目标**：提升模型在 P450 家族上的预测能力

**方法**：
1. **Path B**：构建 P450 专属训练集
   - 从 BRENDA/文献收集 P450 酶-底物对
   - 生成训练/验证/测试集
2. **Path C**：微调 best-checkpoint
   - 在 P450 数据上微调模型
   - 评估性能提升

**预期**：
- AUC-ROC 提升至 0.80+（针对 P450）
- 获得 P450 专属预测能力

**工作量**：2-3 个月

### 5.3 路径 3：数据质量修复

**目标**：排除数据问题对评估的影响

**方法**：
1. **标签验证**：
   - 对低置信度样本进行人工复核
   - 修正错误标签
2. **结构特征检查**：
   - 检查失败的 22 条结构特征（539-517=22）
   - 尝试修复或排除

**预期**：
- 小幅提升评估可靠性
- 为后续研究提供更干净的数据

**工作量**：1 周

### 5.4 推荐优先级

| 优先级 | 路径 | 原因 |
|:------:|------|------|
| **高** | 路径 1 | 成本低、验证核心假说 |
| **高** | 阈值校准 | 使用 0.0379 替代 0.5 可立即提升实用性能 |
| **中** | 路径 2+3 | 如需真正提升 P450 预测效果 |

---

## 附录：关键文件索引

| 文件/目录 | 说明 |
|-----------|------|
| `PathA.../data/04_Step4_CSV格式修正/` | 格式修正后的 CSV |
| `PathA.../data/05_Step5_ESM酶嵌入/` | enzyme_features.lmdb |
| `PathA.../data/06_Step6_反应图/` | reaction_features.lmdb |
| `PathA.../data/07_Step7_分子指纹/` | Morgan + GROVER |
| `PathA.../data/08_Step8_结构特征/` | structure_features.lmdb |
| `PathA.../sessions/09_Step9_模型推理/` | 推理日志 |
| `PathA.../sessions/10_Step10_结果分析/` | 分析报告 |
| `saved_model/model/run_0/best-checkpoint.ckpt` | 模型检查点 |
| `全局进度日志.md` | 项目总体进度 |

---

**文档版本**: v2.0
**最后更新**: 2026-02-01
